<?xml version="1.0" encoding="utf-8"?>
<!--
  The second layer of the reporting gate (the first is BuildProfile.isReportingBuild, which picks
  the no-op reporters). These turn the SDKs' own collection off, so a debug build sends nothing even
  if some other path constructs a real reporter — an SDK that auto-initializes, a library that logs
  on our behalf, or a future binding that forgets the gate.

  Debug and release builds are registered clients of the same Firebase project, so without this a
  development session's events and crashes land in the dashboards that describe shipped builds, and
  quietly make both useless.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
    </application>

</manifest>
